<-(countWords, fn(str,
  <-(count, 0)
  that eval(str) split(" ") each(word,
    <-(count, count +(1))
  )
  count
))

if(argv length() > (0),
  <-(text, argv #(0))
  stdout print(countWords(text))
  stdout print("\n")
,
  stdout print("You must provide an argument to count\n")
  exit(1)
)
